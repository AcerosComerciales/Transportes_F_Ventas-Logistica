Imports System
Imports System.Data
Imports System.Data.Common
Imports System.Collections.Generic

Public Class EUtilitarios
#Region " Variables "
   Public Enum TipoInicializacion
      Todos
      Direcciones
      Telefonos
      Dir_Tel
      EntTipos_Dir_Tel
   End Enum
#End Region

#Region " Propiedades "

#End Region

#Region " Constructores "

#End Region

#Region " Metodos "
   'Public Shared Function getSQL(ByVal x_hash As Hashtable, Optional ByVal x_inicial As Boolean = True) As String
   '   Dim x_sql As String = ""
   '   Try
   '      If x_inicial Then
   '         Dim one As Boolean = True
   '         For Each Item As DictionaryEntry In x_hash
   '            If one Then
   '               x_sql = " " & Item.Key.ToString() & EWhere.getOperador(CType(Item.Value, EWhere).Operador) & CType(Item.Value, EWhere).Valor
   '               one = False
   '            Else
   '               x_sql &= " AND " & Item.Key.ToString() & EWhere.getOperador(CType(Item.Value, EWhere).Operador) & CType(Item.Value, EWhere).Valor
   '            End If
   '         Next
   '      Else
   '         For Each Item As DictionaryEntry In x_hash
   '            x_sql &= " AND " & Item.Key.ToString() & EWhere.getOperador(CType(Item.Value, EWhere).Operador) & CType(Item.Value, EWhere).Valor
   '         Next
   '      End If

   '      Return x_sql
   '   Catch ex As Exception
   '      Throw ex
   '   End Try
   'End Function
#End Region

#Region " Metodos de Controles"

#End Region
End Class



